import styled from '@emotion/styled';
import { colors } from '../../style/colors';
export const InfoSectionContainer = styled.div`
  padding: 23px 1.13rem;
  margin: 0 16px;
  display: flex;
  flex-direction: column;
  border-radius: 1rem;
  background: ${colors.white};
  gap: 40px;
`;
